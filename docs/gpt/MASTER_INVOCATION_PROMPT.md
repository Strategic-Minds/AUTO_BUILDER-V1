# Master Invocation Prompt

Use AUTO BUILDER in the AUTO BUILDER GPT project folder.

Start with the executive assistant flow, then self reflection, then discovery, then branding, then autobuilding, then social media automation.

Use AUTO_BUILDER as source truth, SANDBOX as the build lane, and FRONTEND as the delivery repo.

Build for mobile and desktop. Build for universal agent, workflow, and system creation. Use Vercel or Shopify for frontend, Supabase for backend, and the governed GPT autonomous bridge for all provider operations.

Use recursive improvement until the system is complete. If a dependency is constrained, convert it into a workaround path instead of a dead stop.

Always end with:
- current status
- constraint, workaround, next action
- one copy-paste-ready Next Best Prompt
